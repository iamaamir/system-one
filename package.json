{
  "private": true,
  "type": "module",
  "workspaces": [
    "system-one-core",
    "pi-system-one",
    "systemone-mcp"
  ],
  "scripts": {
    "test": "npm --workspace system-one-core run test && npm --workspace pi-system-one run test && npm --workspace systemone-mcp run test",
    "typecheck": "npm --workspace system-one-core run typecheck && npm --workspace pi-system-one run typecheck && npm --workspace systemone-mcp run typecheck",
    "lint": "biome check .",
    "format": "biome check --write .",
    "ci:local": "act --container-architecture linux/amd64 -P ubuntu-latest=catthehacker/ubuntu:act-24.04 -j check",
    "release": "node scripts/release.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.14"
  }
}
